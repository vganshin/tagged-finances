import { FilterByDepositPipe } from './filter-by-deposit.pipe';

describe('FilterByDepositPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByDepositPipe();
    expect(pipe).toBeTruthy();
  });
});
